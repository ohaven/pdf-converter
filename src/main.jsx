import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
```

**File: `src/App.jsx`**
(This is the long React converter component — paste the entire code I gave you earlier from the "React Component for Electron App" artifact)

**File: `.gitignore`**
```
node_modules/
dist/
.DS_Store
*.log
